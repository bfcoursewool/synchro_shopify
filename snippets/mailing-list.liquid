<div class="footer-mailinglist">

  <div class="mailing-list-signup">
    <div class="mailing-list-image">
      {% if settings.mailing-list-title.size > 0 %}
      <h3 class="mailing-list-title">{{ settings.mailing-list-title }}</h3>
      {% endif %}
    </div>
    <div class="mailing-list-form">
      {% if settings.mailing-list-blurb.size > 0 %}
      <p class="mailing-list-blurb">{{ settings.mailing-list-blurb }}</p>
      {% endif %}
      <div class="klaviyo-form-VfpQK4"></div>
      {% if settings.mailing-list-footnote.size > 0 %}
      <p class="mailing-list-footnote">{{ settings.mailing-list-footnote }}</p>
      {% endif %}
    </div>
  </div>

</div>
